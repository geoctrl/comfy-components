import React, { Component } from 'react';
import { Scoped, k } from 'kremling';

export class TypePage extends Component {
  render() {
    return (
      <Scoped css={css}>
        <div>

        </div>
      </Scoped>
    );
  }
}

const css = k`
  
`;